var Promise = require('native-promise-only');

module.exports = {
  reject: function (msg) {
    console.log('REJECT');
    return Promise.reject(new Error(msg));
  },

  thro: function(msg) {
    throw new Error(msg);
  }
};