var Promise = require('native-promise-only');

module.exports = {
  greet: function (name, cb) {
    //process.nextTick(function() {
      cb(null, 'Hello ' + name);
    //});
  },
  curse: function (name, cb) {
    //process.nextTick(function() {
      cb(null, 'Curse you ' + name + '!');
    //});
  }
};