var Promise = require('native-promise-only');

module.exports = {
  greet: function (name) {
    return new Promise(function (resolve) {
      resolve('Hello ' + name);
    });
  },
  curse: function (name) {
    return new Promise(function (resolve) {
      resolve('Curse you ' + name + '!');
    });
  }
};